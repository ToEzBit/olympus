# Engine HTTP endpoint (Boss command POST /command)
NEXT_PUBLIC_ENGINE_HTTP_URL=http://localhost:3001

# Engine WebSocket endpoint (status + answers stream)
NEXT_PUBLIC_ENGINE_WS_URL=ws://localhost:3002
